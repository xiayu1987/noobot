# Noobot v4.2.9

This release fixes configuration processing and improves cache-hit support for Claude Code requests.

- Repair and normalize configuration consistently across global, default-user, and user scopes.
- Preserve valid user-owned settings while restoring missing or invalid configuration values.
- Use Anthropic's top-level prompt caching contract so Claude Code requests can achieve cache hits.
