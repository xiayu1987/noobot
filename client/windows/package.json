{
  "name": "noobot-windows-client",
  "private": true,
  "version": "4.2.5",
  "type": "module",
  "main": "build/electron/main.js",
  "scripts": {
    "start": "npm run prepare:electron && electron .",
    "dev": "npm run prepare:startup && npm run prepare:electron && node ../shared/electron/dev-launcher.js",
    "prepare:startup": "npm --prefix ../startup run build",
    "prepare:frontend": "npm run prepare:startup && npm --prefix ../noobot-chat run build",
    "prepare:electron": "node ../shared/scripts/prepare-electron-runtime.js",
    "build:win": "npm run prepare:frontend && npm run prepare:electron && npm run prepare:backend && node ../shared/scripts/run-electron-builder.js --win",
    "build:win:dir": "npm run prepare:frontend && npm run prepare:electron && npm run prepare:backend && node ../shared/scripts/run-electron-builder.js --win --dir",
    "check": "node --check src/main.js && npm --prefix ../shared test",
    "prepare:backend": "node scripts/prepare-backend-runtime.js",
    "build:win:portable": "npm run prepare:frontend && npm run prepare:electron && npm run prepare:backend && node ../shared/scripts/run-electron-builder.js --win portable",
    "build:win:zip": "npm run prepare:frontend && npm run prepare:electron && npm run prepare:backend && node ../shared/scripts/run-electron-builder.js --win zip"
  },
  "dependencies": {
    "@noobot/client-shared": "file:../shared",
    "@noobot/runtime-events": "file:../../runtime-events"
  },
  "devDependencies": {
    "electron": "43.4.0",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.2"
  },
  "build": {
    "appId": "com.noobot.windows-client",
    "productName": "Noobot",
    "compression": "store",
    "directories": {
      "output": "dist"
    },
    "files": [
      "build/electron/**/*",
      "assets/**/*",
      "package.json"
    ],
    "win": {
      "icon": "assets/noobot.ico",
      "target": [
        "nsis",
        "zip"
      ]
    },
    "nsis": {
      "installerIcon": "assets/noobot.ico",
      "uninstallerIcon": "assets/noobot.ico",
      "shortcutName": "Noobot"
    },
    "afterPack": "scripts/copy-backend-after-pack.js"
  }
}
