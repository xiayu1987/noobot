{
  "name": "noobot-windows-client",
  "private": true,
  "version": "4.1.3",
  "type": "module",
  "main": "build/electron/main.js",
  "scripts": {
    "start": "npm run prepare:electron && electron .",
    "dev": "npm run prepare:startup && npm run prepare:electron && node ../shared/electron/dev-launcher.mjs",
    "prepare:startup": "npm --prefix ../startup run build",
    "prepare:frontend": "npm run prepare:startup && npm --prefix ../noobot-chat run build",
    "prepare:electron": "node ../shared/scripts/prepare-electron-runtime.mjs",
    "build:win": "npm run prepare:frontend && npm run prepare:electron && npm run prepare:backend && electron-builder --win",
    "build:win:dir": "npm run prepare:frontend && npm run prepare:electron && npm run prepare:backend && electron-builder --win --dir",
    "check": "node --check src/main.js && npm --prefix ../shared test",
    "prepare:backend": "node scripts/prepare-backend-runtime.mjs",
    "build:win:portable": "npm run prepare:frontend && npm run prepare:electron && npm run prepare:backend && electron-builder --win portable",
    "build:win:zip": "npm run prepare:frontend && npm run prepare:electron && npm run prepare:backend && electron-builder --win zip"
  },
  "dependencies": {
    "@noobot/client-shared": "file:../shared",
    "@noobot/runtime-events": "file:../../runtime-events"
  },
  "devDependencies": {
    "electron": "43.2.0",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.1"
  },
  "build": {
    "appId": "com.noobot.windows-client",
    "productName": "Noobot",
    "compression": "store",
    "directories": {
      "output": "dist"
    },
    "files": [
      "build/electron/**/*",
      "assets/**/*",
      "package.json"
    ],
    "win": {
      "icon": "assets/noobot.ico",
      "target": [
        "nsis",
        "zip"
      ]
    },
    "nsis": {
      "installerIcon": "assets/noobot.ico",
      "uninstallerIcon": "assets/noobot.ico",
      "shortcutName": "Noobot"
    },
    "afterPack": "scripts/copy-backend-after-pack.mjs"
  }
}
