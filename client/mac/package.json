{
  "name": "noobot-mac-client",
  "private": true,
  "version": "4.2.1",
  "type": "module",
  "main": "build/electron/main.js",
  "scripts": {
    "start": "npm run prepare:electron && electron .",
    "dev": "npm run prepare:startup && npm run prepare:electron && node ../shared/electron/dev-launcher.js",
    "prepare:startup": "npm --prefix ../startup run build",
    "prepare:frontend": "npm run prepare:startup && npm --prefix ../noobot-chat run build",
    "prepare:backend": "node scripts/prepare-backend-runtime.js",
    "prepare:electron": "node ../shared/scripts/prepare-electron-runtime.js",
    "check": "node --check src/main.js && npm --prefix ../shared test",
    "build:mac": "npm run prepare:frontend && npm run prepare:electron && npm run prepare:backend && node ../shared/scripts/run-electron-builder.js --mac",
    "build:mac:dir": "npm run prepare:frontend && npm run prepare:electron && npm run prepare:backend && node ../shared/scripts/run-electron-builder.js --mac --dir",
    "build:mac:dmg": "npm run prepare:frontend && npm run prepare:electron && npm run prepare:backend && node ../shared/scripts/run-electron-builder.js --mac dmg",
    "build:mac:zip": "npm run prepare:frontend && npm run prepare:electron && npm run prepare:backend && node ../shared/scripts/run-electron-builder.js --mac zip"
  },
  "dependencies": {
    "@noobot/client-shared": "file:../shared",
    "@noobot/runtime-events": "file:../../runtime-events"
  },
  "devDependencies": {
    "electron": "43.4.0",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.2"
  },
  "build": {
    "appId": "com.noobot.mac-client",
    "productName": "Noobot",
    "compression": "store",
    "directories": {
      "output": "dist"
    },
    "files": [
      "build/electron/**/*",
      "assets/**/*",
      "package.json"
    ],
    "mac": {
      "icon": "assets/noobot.icns",
      "target": [
        "zip"
      ],
      "category": "public.app-category.productivity"
    },
    "afterPack": "scripts/copy-backend-after-pack.js"
  }
}
